# Docker configuration
# Copy environment variables from back/.env and front/.env as needed
# This file should reference or be merged with the individual .env files

# You can set override variables here, or rely on the env_file directives
# in compose.dev.yaml that reference ./back/.env and ./front/.env
